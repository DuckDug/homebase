services:
  postgres:
    image: postgres:16
    container_name: homebase-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: homebase
    ports:
      - "5432:5432"
    volumes:
      - homebase-pgdata:/var/lib/postgresql/data

volumes:
  homebase-pgdata: